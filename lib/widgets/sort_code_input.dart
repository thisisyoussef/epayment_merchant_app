import 'package:flutter/material.dart';

class SortCodeInput extends StatelessWidget {
  const SortCodeInput({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
